__pycache__/
*.pyc
.DS_Store
*.bak
~$*
.venv/
.venv-container/
.playwright-mcp/

# Бэкапы analytics (любое подчёркивание во внутренней части имени файла = backup)
analytics_report_*_*.xlsx
# Старый xlsx-дамп с датой в имени — не используется loader'ом
analytics_report_2*-*-*.xlsx

# Рабочие скриншоты для аудита дашборда (создаются playwright-проверками)
audit-*.png
verify-*.png
dashboard-*.png
dashboard-*.jpeg
cloud-*.png
unit-*.png
page-*.png

# Дубликаты юнит-файлов в корне — они должны лежать только в data/unit_economics/
/Юнит-экономика_*.xlsx

# Локальный файл секретов Streamlit (если когда-нибудь появится)
.streamlit/secrets.toml
