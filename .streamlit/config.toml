[server]
headless = true
enableCORS = false
enableXsrfProtection = false
# address/port не задаём — Streamlit Cloud управляет ими через переменные окружения.
# Локально run.sh передаёт --server.port явно, дефолт 8501.

[browser]
gatherUsageStats = false
